import math

class Mass:
    # Basic particles
    electron = 9.1 * (math.pow(10, -31))
    proton = 1.67 * (math.pow(10, -27))
    neutron = 1.67 * (math.pow(10, -27))

    # Atoms
    hydrogen = 1.01 * (math.pow(10, -27))
    helium = 6.64 * (math.pow(10, -27))
    lithium = 1.15 * (math.pow(10, -26))
    beryllium = 1.53 * (math.pow(10, -26))
    boron = 2.34 * (math.pow(10, -26))
    carbon = 2.33 * (math.pow(10, -26))
    nitrogen = 2.32 * (math.pow(10, -26))
    oxygen = 2.66 * (math.pow(10, -26))
    fluorine = 3.14 * (math.pow(10, -26))
    neon = 6.64 * (math.pow(10, -27))
    sodium = 2.32 * (math.pow(10, -26))
    magnesium = 3.86 * (math.pow(10, -26))
    aluminum = 4.48 * (math.pow(10, -26))
    silicon = 4.68 * (math.pow(10, -26))
    phosphorus = 3.9 * (math.pow(10, -26))
    sulfur = 4.82 * (math.pow(10, -26))
    chlorine = 5.97 * (math.pow(10, -26))
    argon = 6.64 * (math.pow(10, -27))
    potassium = 3.75 * (math.pow(10, -26))
    calcium = 5.01 * (math.pow(10, -26))
    scandium = 1.17 * (math.pow(10, -25))
    titanium = 1.48 * (math.pow(10, -25))
    vanadium = 1.51 * (math.pow(10, -25))
    chromium = 1.56 * (math.pow(10, -25))
    manganese = 1.74 * (math.pow(10, -25))
    iron = 1.87 * (math.pow(10, -25))
    nickel = 1.84 * (math.pow(10, -25))
    copper = 1.15 * (math.pow(10, -24))
    zinc = 1.45 * (math.pow(10, -24))
    gallium = 2.29 * (math.pow(10, -24))
    germanium = 2.32 * (math.pow(10, -24))
    arsenic = 2.27 * (math.pow(10, -24))
    selenium = 2.68 * (math.pow(10, -24))
    bromine = 2.96 * (math.pow(10, -24))
    krypton = 6.64 * (math.pow(10, -27))
    rubidium = 4.18 * (math.pow(10, -24))
    strontium = 5.01 * (math.pow(10, -24))
    yttrium = 1.58 * (math.pow(10, -24))
    zirconium = 1.68 * (math.pow(10, -24))
    niobium = 1.78 * (math.pow(10, -24))
    molybdenum = 1.87 * (math.pow(10, -24))
    technetium = 1.85 * (math.pow(10, -24))
    ruthenium = 1.89 * (math.pow(10, -24))
    rhodium = 1.95 * (math.pow(10, -24))
    palladium = 2.24 * (math.pow(10, -24))
    silver = 1.79 * (math.pow(10, -24))
    cadmium = 2.28 * (math.pow(10, -24))
    indium = 2.36 * (math.pow(10, -24))
    tin = 2.89 * (math.pow(10, -24))
    antimony = 2.98 * (math.pow(10, -24))
    tellurium = 3.4 * (math.pow(10, -24))
    iodine = 3.95 * (math.pow(10, -24))
    xenon = 6.64 * (math.pow(10, -27))
    cesium = 4.48 * (math.pow(10, -24))
    barium = 5.02 * (math.pow(10, -24))
    lanthanum = 1.85 * (math.pow(10, -24))
    cerium = 1.8 * (math.pow(10, -24))
    praseodymium = 1.83 * (math.pow(10, -24))
    neodymium = 1.82 * (math.pow(10, -24))
    promethium = 1.68 * (math.pow(10, -24))
    europium = 1.35 * (math.pow(10, -24))
    gadolinium = 1.8 * (math.pow(10, -24))
    terbium = 1.59 * (math.pow(10, -24))
    dysprosium = 1.72 * (math.pow(10, -24))
    holmium = 1.65 * (math.pow(10, -24))
    erbium = 1.68 * (math.pow(10, -24))
    thulium = 1.74 * (math.pow(10, -24))
    ytterbium = 1.75 * (math.pow(10, -24))
    lutetium = 1.71 * (math.pow(10, -24))
    hafnium = 1.78 * (math.pow(10, -24))
    tantalum = 1.8 * (math.pow(10, -24))
    tungsten = 1.83 * (math.pow(10, -24))
    rhenium = 1.88 * (math.pow(10, -24))
    osmium = 1.9 * (math.pow(10, -24))
    iridium = 1.92 * (math.pow(10, -24))
    platinum = 1.94 * (math.pow(10, -24))
    gold = 3.27 * (math.pow(10, -25))
    mercury = 2.99 * (math.pow(10, -25))
    thallium = 3.12 * (math.pow(10, -24))
    lead = 3.18 * (math.pow(10, -24))
    bismuth = 3.27 * (math.pow(10, -24))
    polonium = 3.4 * (math.pow(10, -24))
    astatine = 3.75 * (math.pow(10, -24))
    radon = 6.64 * (math.pow(10, -27))
    francium = 3.48 * (math.pow(10, -24))
    radium = 5.02 * (math.pow(10, -24))
    actinium = 1.9 * (math.pow(10, -24))
    thorium = 1.77 * (math.pow(10, -24))
    protactinium = 1.68 * (math.pow(10, -24))
    uranium = 4.48 * (math.pow(10, -25))
    neptunium = 1.68 * (math.pow(10, -24))
    plutonium = 1.65 * (math.pow(10, -24))
    americium = 1.73 * (math.pow(10, -24))
    curium = 1.68 * (math.pow(10, -24))
    berkelium = 1.68 * (math.pow(10, -24))
    californium = 1.68 * (math.pow(10, -24))
    einsteinium = 1.68 * (math.pow(10, -24))
    fermium = 1.68 * (math.pow(10, -24))
    mendelevium = 1.68 * (math.pow(10, -24))
    nobelium = 1.68 * (math.pow(10, -24))
    lawrencium = 1.68 * (math.pow(10, -24))
    rutherfordium = 1.68 * (math.pow(10, -24))
    dubnium = 1.68 * (math.pow(10, -24))
    seaborgium = 1.68 * (math.pow(10, -24))
    bohrium = 1.68 * (math.pow(10, -24))
    hassium = 1.68 * (math.pow(10, -24))
    meitnerium = 1.68 * (math.pow(10, -24))
    darmstadtium = 1.68 * (math.pow(10, -24))
    roentgenium = 1.68 * (math.pow(10, -24))
    copernicium = 1.68 * (math.pow(10, -24))
    nihonium = 1.68 * (math.pow(10, -24))
    flerovium = 1.68 * (math.pow(10, -24))
    moscovium = 1.68 * (math.pow(10, -24))


    water_molecule = 2.99 * (math.pow(10, -26))    # Mass of a water molecule (H2O)
    methane_molecule = 4.65 * (math.pow(10, -26))   # Mass of a methane molecule (CH4)
    carbon_dioxide_molecule = 7.3 * (math.pow(10, -26))  # Mass of a carbon dioxide molecule (CO2)
    glucose_molecule = 1.8 * (math.pow(10, -25))    # Approximate mass of a glucose molecule (C6H12O6)
    caffeine_molecule = 1.2 * (math.pow(10, -24))    # Approximate mass of a caffeine molecule (C8H10N4O2)

    # Ions
    sodium_ion = 2.32 * (math.pow(10, -26))       # Mass of a sodium ion (Na+)
    chloride_ion = 2.33 * (math.pow(10, -26))     # Mass of a chloride ion (Cl-)
    potassium_ion = 3.75 * (math.pow(10, -26))    # Mass of a potassium ion (K+)
    nitrate_ion = 4.68 * (math.pow(10, -26))      # Mass of a nitrate ion (NO3-)

    # Complex structures
    DNA_double_helix = 1.6 * (math.pow(10, -21))  # Approximate mass of a DNA double helix
    protein_insulin = 5.8 * (math.pow(10, -25))   # Approximate mass of an insulin protein
    protein_hemoglobin = 6.4 * (math.pow(10, -25))  # Approximate mass of a hemoglobin protein

    # Subatomic particles
    muon = 1.88 * (math.pow(10, -28))  # Mass of a muon
    tau_particle = 3.16 * (math.pow(10, -27))  # Mass of a tau particle
    neutrino_electron = 1e-36  # Mass of an electron neutrino

    # Exotic particles
    top_quark = 3.1 * (math.pow(10, -25))  # Mass of a top quark
    Z_boson = 9.46 * (math.pow(10, -25))  # Mass of a Z boson
    dark_matter_particle = 1e-35  # Mass of a hypothetical dark matter particle
    graviton = 0  # Theoretical mass of a graviton (still hypothetical)
    tachyon = -1e-36  # Theoretical mass of a tachyon (still hypothetical)